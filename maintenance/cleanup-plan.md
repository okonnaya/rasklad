# План уборки «Расклада»

11 сентября 2026. Сохраняем тексты, дизайн и URL, кроме согласованных правок футера.

1. Обновить футер: HSE ADC → звезда → настя м., карина р., маша в.; новые ссылки.
2. Исправить поиск (отсутствующие поля, нормализация, повторный рендер), ссылки
   статей и раскладов, случайный импорт поиска в каталоге, проверки DOM и аналитики.
3. Обновить совместимые зависимости и метаданные проекта, оставить npm lockfile.
   Удалить неиспользуемые Airtable/axios, deprecated Babel plugin и file-loader.
4. Упростить Webpack: убрать дубли страниц, включить минификацию JS и общие chunks,
   исключить включение всех JSON через динамический require изображений.
5. Удалить подтверждённо мёртвые JS-заглушки и лишний jQuery, объединить код раскладов,
   привести JS к единому стилю. Отдельный Python-прототип сохранить вне сборки.
6. Lint, проверки данных/поиска/выборки, production build, проверка ресурсов и ссылок,
   браузерные проверки поиска, карты дня, каталогов и мобильного меню.
7. Сравнить размер сборки, записать результаты, опубликовать проверенные изменения.

## Подтверждённые проблемы до изменений

- search.jsx: similarWords.replaceAll вызывается для отсутствующего поля.
- cards.jsx: импорт search.jsx запускает код поиска в каталоге без поискового DOM.
- articles.js: фильтр читает children у отсутствующего контейнера.
- У статей и раскладов отсутствуют link; карточки отображаются без адресов.
- Динамический require всей data включает лишние JSON в браузерную сборку.
- contentrandomizer.js подменяет настоящие названия карточек случайным текстом.
- Webpack: 98 объявлений страниц, 95 уникальных; старые файлы не очищаются.
- optimization.minimizer заменяет минификатор JS единственным CSS-плагином.
- Npm audit: 39 записей — 2 critical, 18 high, 14 moderate, 5 low.
- JS главной: 532 340 байт (gzip 146 923); каталога: 575 031 (gzip 152 642).
  Учтены локальные подключённые скрипты, внешняя аналитика не включена.

Chrome DevTools MCP недоступен: Core Web Vitals/Lighthouse не измеряются.
Проверяем размеры production-ресурсов и поведение в доступном браузере.
